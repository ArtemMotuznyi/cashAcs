# JWT Secret Template
# Copy this file to /run/secrets/jwt_secret and replace with a strong random secret
# MUST be at least 32 characters long for security

# Generate a strong secret using:
# openssl rand -hex 32
# or
# openssl rand -base64 48

replace_with_strong_random_secret_at_least_32_characters_long_for_production_use