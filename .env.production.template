# Production Environment Configuration Template
# Copy this file to .env.production and fill in the actual values

# ======================
# Database Configuration
# ======================
DATABASE_URL=jdbc:postgresql://your-postgres-host:5432/ktor_db
DATABASE_USER=ktor_user_prod
DATABASE_PASSWORD=your_very_strong_database_password_here

# ======================
# OAuth Configuration
# ======================
# Update this to your actual production domain
OAUTH_REDIRECT_URI=https://your-domain.com/oauth2callback
ALLOWED_HOST=your-domain.com

# ======================
# Application Security
# ======================
# Path to admin credentials file (should contain username on line 1, bcrypt hash on line 2)
ADMIN_CRED_FILE=/run/secrets/admin_credentials

# Path to Google OAuth client secret JSON file
CLIENT_SECRET=/run/secrets/client_secret

# Path to master key file for ua.developer.artemmotuznyi.token encryption (should be 64+ characters)
MAIL_TOKEN_MASTER_KEY=/run/secrets/master_key

# ======================
# Optional Configuration
# ======================
# Application port (default: 8080)
PORT=8080

# PgAdmin configuration (for development only)
PGADMIN_EMAIL=admin@your-domain.com
PGADMIN_PASSWORD=your_pgadmin_password